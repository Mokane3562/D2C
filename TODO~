- Add calls to the sql statements (through the SQLHandler) to get the results
	from the SQL selects/updates/inserts for each WebAPI call that POSTs
	or GETs information from the database.
- Using the c_compile_request, c_compile_service, and 
	CodeResource.compileCCode path as an example, make the angular services
	required to call all Resource Class methods.
- Make sure passwords are stored in a secure way on the database. It seems a 		common way to do this is to hash them first.


In the Documentation folder, place our (Updated) higher level design pdf.
